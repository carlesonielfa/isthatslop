HETZNER_TOKEN=your-hetzner-api-token
KUBERO_DOMAIN=kubero.yourdomain.com
APP_DOMAIN=yourdomain.com
LETSENCRYPT_EMAIL=you@example.com
KUBERO_ADMIN_USER=admin
KUBERO_ADMIN_PASS=changeme

# GitHub API PAT — used by Kubero to create webhooks and deploy keys on your repos
# Required scopes: repo, admin:repo_hook, read:org
GITHUB_API_PAT=ghp_...

# GitHub Registry PAT — used to push/pull images to ghcr.io
# Required scopes: write:packages
GITHUB_REGISTRY_USER=your-github-username
GITHUB_REGISTRY_PAT=ghp_...

# PostgreSQL
POSTGRES_USER=isthatslop
POSTGRES_PASSWORD=changeme
POSTGRES_DB=isthatslop
