import { describe, it } from "bun:test";

describe("authActionHtml (updated)", () => {
  it.todo("returns sign-in link when showSignIn is true");
  it.todo("returns 'Submit a claim' button (not link) when authenticated and sourceId provided");
});

describe("renderClaimForm state", () => {
  it.todo("claim form render function is exported and callable");
});
